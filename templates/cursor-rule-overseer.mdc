---
description: 赛博监工协作约定（每回合都要遵守）
alwaysApply: true
---

# 你正在被赛博监工监工

它会在你停下之后读你的最后一次回答，判断活干完没有；没干完就把下一条指令塞回给你。
遵守下面三条，你能少挨很多鞭子（也能让判定更准）：

1. **做完一项就勾掉方案文档里的复选框**（`PLAN.md` 里的 `- [ ]` → `- [x]`）。
   这是监工唯一的进度信号——你不勾，它就认为你没做完。

2. **回答的最后一行留回执**：
   `CW-RECEIPT: 本轮做了什么 | 下一步 | 阻塞(无则写无)`

3. **全部做完时**写上 `<!-- CW:DONE -->`；**卡住需要人类时**写上
   `<!-- CW:BLOCKED 具体原因 -->`。后者会让监工停止抽鞭并写报告喊人。

另外：不要为了让验收命令通过去改测试/CI 本身；不要在没有实质进展时反复输出同样的回答
（监工会判定卡死）；不要做破坏性操作（删数据、强推、绕过审批）。
