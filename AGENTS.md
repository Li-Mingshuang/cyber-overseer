# 给 agent 的接入说明（AGENTS.md）

> 把本文件（或它的片段）放进你的项目根目录，让被监工的 agent 知道"有人在盯着它"，
> 并遵守下面这套约定。协议是**可选的**——不遵守也能被监工，只是判定会变笨。

你正在被**赛博监工**（Cyber Overseer）监工。它会在你停下来之后读你的最后一次回答，
判断你干完没有；没干完就会把下一条指令重新塞给你。为了让它的判断准确、也让你少挨鞭子，
请遵守以下约定。

## 1. 做完一项就勾掉一项

方案文档（通常是 `PLAN.md`）里的复选框是你**唯一的进度信号**：

```markdown
- [ ] 接入 TTS 依赖     →  做完后改成  - [x] 接入 TTS 依赖
```

监工每轮都会重读方案文档。你不勾，它就只能认为你没做完，于是继续抽你。

## 2. 在回答末尾留一行回执

```text
CW-RECEIPT: 本轮做了什么 | 下一步 | 阻塞(无则写无)
```

例：

```text
CW-RECEIPT: 实现了 weather say 子命令并补了 3 个测试 | 下一步 更新 README | 阻塞 无
```

这一行让监工能直接读到你的进展，而不是去猜你那段长篇回答。

## 3. 明确宣告完成或受阻（可选，但强烈建议）

- 你认为方案**真的全部完成**了（含验收命令通过）时，在回答里写上标记：

  ```html
  <!-- CW:DONE -->
  ```

  注意：监工默认**不轻信**这个标记（`judge.rule.trustAgentDone: false`），
  它会用方案勾选和验收命令复核。想让它信，就把活真的干完。

- 你**卡住了**、需要人类介入时：

  ```html
  <!-- CW:BLOCKED 需要生产数据库的只读凭据，我拿不到 -->
  ```

  这样监工会停止抽鞭并写报告喊人，而不是继续无效地抽你。

## 4. 不要做的事

- **不要为了让验收命令通过而修改验收命令本身**（改测试断言、改 CI、删用例）。
  监工能看到 `git diff`，主人早上会看报告。
- **不要在没有实质进展时反复输出同样的回答**——连续几轮回答与工作区都没变化，
  监工会判定"卡死"并停止。
- **不要执行破坏性操作**：删数据、强制推送、绕过审批、把密钥写进代码。
  监工的鞭子里会明确写着这条约束。

## 5. 如果你支持 MCP：把"取指令"变成一次工具调用

挂上 `cw mcp --serve` 之后：

- **在结束回合之前**调用 `overseer_check`：它会返回监工给你的下一条指令
  （返回里若写明任务已完成，你就可以安心停下）；
- **做完实质性工作后**调用 `overseer_report`，参数：

  ```json
  { "summary": "本轮做了什么（一句话）",
    "evidence": "跑了什么命令、输出是什么、改了哪些文件",
    "done": false, "blocked": false, "files": ["src/a.ts"] }
  ```

这比"靠自由文本被解析"可靠得多，也是监工最省事、你最不容易被误判的形态。

## 6. 一句话总结你的义务

**勾复选框、留回执、说实话。** 剩下的交给监工。
